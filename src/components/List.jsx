import styled from "styled-components";

export default function List() {
  return <ListWrapper>adsa</ListWrapper>;
}

const ListWrapper = styled.div`
  width: 200px;
  height: 300px;
  background-color: black;
`;
